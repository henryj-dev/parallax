# 할 일 — 2026-08-24 코드 전수 점검 + 재검증

[`security-audits/2026-08-24-code-only-audit.md`](../security-audits/2026-08-24-code-only-audit.md)
(본문 스냅샷 · 정정 6건 · 추가 7건)에서 나온 작업 목록이다. **왜 그런지는 그 리포트에,
어떻게 할지는 [`2026-08-24-remediation-plan.md`](../security-audits/2026-08-24-remediation-plan.md)
의 같은 번호에 있다.** 여기는 체크하는 곳이다.

> 📋 **체크는 이 파일에서만 한다.** [`docs/todo.md`](todo.md) 에는 이 파일을 가리키는
> 한 줄만 있다 — 두 곳에 적힌 상태는 반드시 어긋난다.

> 🔑 **「Critical/High 없음」은 유지된다.** 재검증도 그 판정을 뒤집지 못했다. 아래는
> ① 결론이 아니라 근거·범위가 흔들린 자리와 ② 아직 아무도 안 본 축이다.

크기: **XS** 한 줄~다섯 줄 · **S** 한 함수 · **M** 한 모듈 · **L** 설계가 필요.

---

## 1단계 — 쿠키 두 건

- [x] **A1** · `L` · 🔴 세션 쿠키가 bearer 토큰 **원문**을 담는다 —
      `src/security/http-authorization.ts:321` · `:327` · `:296` · `:495`
      <br>✅ **D-A1 결정:** 별도 영속 secret 기반 서명 세션(㉠)을 적용했다.
      (계획 §A1 표). 2026-08-22 **L6** 의 열린 절반이라 보류를 뒤집는 판단이 앞선다
      <br>**완료:** ① 쿠키 값을 그대로 `Authorization: Bearer` 로 보내면 **401**
      ② `DELETE` 뒤 **같은 쿠키 값을 다시 제시해도 401** ← 이쪽이 본체
      <br>✅ `PARALLAX_SESSION_SECRET`(최소 32바이트)을 모든 replica 에 영속적으로 설정한다.
      미설정 배포는 토큰 세션 발급을 거부하며, 설정 변경은 전원 로그아웃이다
      <br>⚠️ 세션 폐기 목록은 현재 프로세스 메모리다 — replica 간 **즉시** 폐기까지
      필요하면 공유 저장소가 별도 결정을 필요로 한다
      <br>📌 포털은 이 쿠키를 안 읽는다(HttpOnly, `public/api-client.js:26`) — 파급은 서버 안

- [x] **A2** · `S` · 세션 쿠키에 `__Host-` 접두사가 없다 —
      발급 `src/http/identity-routes.ts:115` · `:118` ·
      `src/security/http-authorization.ts:327` / 삭제 `identity-routes.ts:131` ·
      `http-authorization.ts:337` / 읽기 `:495` · `:507` · `identity-routes.ts:130`
      <br>`handshakeName()`(`identity-routes.ts:169`)과 **똑같은 조건부**(https 일 때만)
      <br>**완료:** https 에서 세 쿠키 전부 `__Host-` + `Secure` + `Path=/` + `Domain` 없음.
      plain-HTTP loopback 은 지금 이름 그대로
      <br>**테스트:** https 발급 / http 발급 / 발급한 쿠키로 인증 성립(읽기 이름이 따라갔나) /
      **삭제 쿠키도 같은 이름**(어긋나면 로그아웃이 아무것도 안 지운다)
      <br>⚠️ `cookieName` 이 설정 가능(`:446`) — 이미 `__Host-` 로 시작하면 이중 접두사 금지
      <br>⚠️ **배포 시 전원 1회 로그아웃.** 옛 이름을 계속 받아 주면 shadowing 이 그대로다 —
      릴리스 노트에 적을 것
      <br>🔑 **A1 과 독립이고 더 싸다.** A1 결정이 늦으면 이것부터

## 2단계 — OIDC

- [x] **A3** · `M` · ID token `iss` · `aud` · `exp` 검증 + **UserInfo `sub` 대조** —
      `src/security/oidc.ts:201`(`exchangeCode`) · `:244`(`readIdentity`)
      <br>`sub` 대조는 OIDC Core §5.3.2 의 **MUST** 이고 지금은 userinfo 의 `sub` 만 읽는다
      <br>✅ `id_token` 을 필수화하고, 검증 실패 시 로그인을 거부했다
      <br>**테스트:** 다른 `aud` / 다른 `iss` / 만료된 `exp` / **`sub` 불일치** ← 본체 /
      정상값은 지금과 동일 통과
      <br>📌 서명 검증(JWKS)은 이 항목에 넣지 않는다 — token endpoint TLS 로 갈음 가능
      (OIDC Core §3.1.3.7). **다만 그 예외가 위 셋을 면제해 주지는 않는다**

- [x] **A4** · `S` · `nonce` — **발행이 먼저다** — `src/security/oidc.ts:184` ·
      콜백 `src/http/identity-routes.ts:95-110`
      <br>✅ 인가 요청에 nonce 를 넣고 핸드셰이크 쿠키로 보관한 뒤 ID token 과 대조한다
      <br>**완료:** 저 grep 이 0건이 아니게 되고, 다른 `nonce` / `nonce` 없는 ID token 이 거부
      <br>⚠️ **A3 뒤에.** 이 계획에서 강제 순서가 있는 유일한 쌍 — 대조할 자리가 없으면
      발행해도 아무것도 안 산다
      <br>⚠️ 핸드셰이크 쿠키가 하나 는다 — A2 의 이름 규칙을 따를 것

## 3단계 — 관측성

- [x] **A5** · `S` · DNS forwarding 을 **upstream 별·사유별**로 — `src/dns/server.ts:1261`
      <br>세 갈래 `continue` + relay 실패가 전부 같은 침묵. 지금 남는 건
      `outcome: answered|failed` 둘뿐(`:367`)
      <br>**나눌 것:** `resolve` · `address_refused` · `budget` · `connect_send` ·
      `correlation` · `timeout` → `src/observability/signals.ts`
      <br>⚠️ **라벨에 호스트·질의 이름 금지**(`signals.ts:75-82` 의 근거). upstream 을
      값으로 넣을지 인덱스로 넣을지 정하고 갈 것
      <br>**테스트:** 가짜 `resolveHost`/relay 로 여섯 갈래를 하나씩, `render()` 에서 확인
      <br>📌 근거는 「간헐 실패」가 아니라 **「관측 불가」** — 그 간헐 실패는 재현되지 않았다

- [x] **A6** · `XS` · 인증 실패 제한을 **관측성 항목으로 격하** (코드 변경 없음)
      <br>우회는 되지만 **얻는 것이 없다**(`http-authorization.ts:385` 가 그렇게 적어 놨다).
      `Map` 은 10,000 상한 + LRU 로 이미 유계(`:418`)
      <br>**완료:** 다음 검수가 이걸 취약점으로 다시 올릴 때 반박할 줄이 있다
      <br>📌 실제 방어는 `MIN_TOKEN_BYTES = 32` 와 `isStrongBootstrapToken` — 그게 약해지면
      이 항목 등급도 같이 오른다

## 4단계 — 아무도 안 본 일곱 축

> §점검 개요는 대상에 "실행 스크립트, 설정 코드" 를 넣었는데 본문 언급이 **0회**다.
> **완료 정의는 공통: 계획서 §부록 A 에 「무엇을 봤고 무엇이 나왔나」를 적는다.**
> 아무것도 안 나왔으면 그렇게 적는다 — 그게 미점검과 점검을 가르는 유일한 표시다.

- [x] **A7** · `pnpm run audit` — 원 점검은 **네트워크 차단으로 실행 불가**였고, 그 한정이
      §검증 결과 안쪽에만 있어 결론이 의존성까지 덮는 것처럼 읽혔다
      <br>📌 **지금 바로.** 결론에 붙는 한정을 뗄 수 있는지가 여기 달렸다.
      프로덕션 의존성은 `pg` 하나 — 짧은 것과 없는 것은 다르다
- [x] **A8** · `migrations/` 5개 — 특히 `004_security_invariants.sql` ·
      `005_access_token_lifetime.sql`. 불변식이 실제 제약인가 · 재실행 안전한가 ·
      앞 버전에서 올라오는 경로가 있는가
- [x] **A9** · `Dockerfile` — 이미지 빌드와 **uid·권한**. CI `docker` 잡이 이미 답하고
      있으니 **그 잡의 주장부터 읽을 것**
- [x] **A10** · `.github/workflows/**` 다섯 — `permissions:` 범위 ·
      `pull_request_target` 사용 여부 · 서드파티 액션 핀 방식 · 포크 PR 에 시크릿이 닿는 경로
- [x] **A11** · `scripts/claude-hooks/**` · `scripts/git-hooks/**` — CI(`scripts.yml`)에서
      **실제로 도는** 파이썬 스위트와 shellcheck
      <br>⚠️ **stardust 의 스냅샷이다.** 결함을 찾으면 **고칠 자리는 stardust** 이고 여기로
      다시 뜨는 것 — 여기서 고치면 다음 스냅샷이 덮는다
- [x] **A12** · 포털 브라우저 실조작 — 실제 Chrome에서 로컬 포털을 실행해 확인했다
      <br>✅ 악성 레코드 값과 토큰 subject에 `<img src=x onerror=…>`를 저장한 뒤 존 상세·
      레코드 목록·revision·감사·토큰 화면을 확인했다. 값은 텍스트로 보였고 실제 `img`·
      `onerror`는 생성되지 않았으며 probe 실행 횟수는 0이었다
      <br>✅ `/?signin_error=...`도 인증 오류 영역에 텍스트로 표시됐고 URL에서 제거됐다.
      `img` 0개·`onerror` 속성 0개·콘솔 error 0건을 확인했다
- [x] **A13** · `pnpm verify:cloudflare` — 실제 자격증명 필요. 없으면 **「자격증명이 없어
      못 돌렸다」를 적는다** — 상태는 같아도 다음 사람이 같은 걸 다시 발견하지 않는다

## 5단계 — 같은 착오가 반복되지 않게

- [x] **A14** · `XS`~`S` · 스킵을 **찾는 방법**을 고친다
      <br>스킵 3건은 **셋 다 정당하다.** 틀린 건 탐지 방법 — 문자열 grep 은 node:test 의
      옵션 형태(`{ skip: … }`)와 컨텍스트 형태(`context.skip()`)를 구조적으로 못 본다.
      게다가 `pnpm test` 요약의 `skipped 3` 은 **grep 없이도 보였어야 했다**
      <br>택1: ① `docs/` 에 현재 셋과 사유를 적고 검수 절차에 대조를 넣는다 `XS` /
      ② 알려진 스킵 수와 다르면 실패하는 테스트 `S`
      <br>📌 현재 셋: `test/infrastructure/atomic-file.test.ts:103` · `:122`
      (`{ skip: linuxOnly() }`, 리눅스 CI 에서는 실제로 돈다) ·
      `test/adapters/provider-contract.test.ts:75`(`context.skip`, 사유 `:307`)
- [x] **A15** · `S` · 배포 가정을 문서에 적는다 (셋 다 코드 주석에만 있다)
      <br>① `servedByProvider` 릴레이가 `forwardAllow` 밖(`src/dns/server.ts:412-419`) —
      **단순 오픈 리졸버가 아니다**(서빙 존의 placeholder 이름만, rate limiter `:543`·`:572`,
      `maxConcurrentForwards` `:356`). **그러나 `forwardTo` 가 내부 DNS 면** 외부
      클라이언트에게 제한적 내부 DNS 관측면이 열린다 → 위협 모델과 허용 placeholder 범위 명시
      <br>② **존별 RBAC 없음**(`http-authorization.ts:149`) — 인가의 축이 존이 아니라 라우트
      <br>③ `parallax_oidc_id` 는 API 자격증명은 아니지만 민감한 인증 결과이고 logout 시
      IdP 로 전달된다(`identity-routes.ts:115-118`) — 셋을 같은 "세션 쿠키" 로 묶지 말 것
- [x] **A16** · `XS` · `docs/todo.md` 에서 이 파일로 오는 줄 + **결론에 한정 붙이기**
      <br>「Critical/High 없음」은 `src/`·`public/`·`test/` 를 **정적으로 읽은 범위**에
      대한 것 — 의존성(A7) · 포털 실조작(A12) · Cloudflare 실경로(A13) · 네 축(A8~A11)은
      **점검되지 않았다.** A3 전까지 OIDC 결론에도 같은 한정이 붙는다

---

## 사람이 정해야 하는 것

- [x] **D-A1** · A1 을 **어느 방식으로**, 그리고 애초에 할 것인가
      <br>✅ ㉠ 별도 영속 secret 기반 서명 세션을 선택했다. `PARALLAX_SESSION_SECRET`을
      새 환경변수로 사용하고, 모든 replica 가 같은 값을 공유한다
      <br>⚠️ 폐기 목록은 프로세스별이다. replica 간 즉시 폐기가 요구되면 공유 저장소를
      추가로 결정해야 한다
      <br>⚠️ **OIDC 세션 즉시 폐기(원 보고서 권고 3)는 여기 딸려 오지 않는다** — 보류
      결정이 유효하고 재검증도 뒤집을 근거를 대지 못했다
- [x] **D-A2** · 존별 RBAC — 이전 리포트가 사람에게 넘겼고 「하지 않기로 한 것」에 있다.
      **결론을 바꿀 필요는 없다.** 다만 A15 로 **명시**되어야 한다 — 명시되지 않으면
      없는 것처럼 읽힌다

---

## 커밋 전 게이트

```
pnpm check && pnpm run check:portal && pnpm build && pnpm test
```

- **A5** 는 `pnpm verify:dns` 추가 — Docker 도 네트워크도 안 쓴다
- **A2** 는 배포 후 **실제 브라우저에서 한 번 로그인해 볼 것** — 쿠키 이름 변경은 단위
  테스트가 다 통과해도 브라우저가 거부할 수 있는 종류다
- 푸시 후 CI 다섯이 초록일 것. ⚠️ **A11 이 `scripts.yml` 을 건드리면 그 잡이 바로 답한다**
